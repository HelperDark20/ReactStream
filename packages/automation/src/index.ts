/**
 * @reactstream/automation
 * Lógica de validación de automatizaciones en TS (compartida entre editor UI y tests de contrato).
 *
 * TODO(Etapa 1 — Contracts): implementar contratos reales.
 */

export {};
