{
  "name": "@reactstream/types",
  "version": "0.1.0",
  "private": true,
  "description": "Tipos compartidos transversales (Automation, Action, Session, License, protocolos).",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@reactstream/events": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^4.1.11"
  }
}
