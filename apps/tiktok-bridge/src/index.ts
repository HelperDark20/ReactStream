/**
 * ReactStream — TikTok Bridge
 *
 * Responsabilidades permitidas (Sección 24): conectar/desconectar/reconectar
 * TikTok, recibir y normalizar eventos, enviarlos al Core por WebSocket local,
 * reportar estado y errores.
 *
 * PROHIBIDO en este proceso (Regla inmutable #5-13): automatizaciones,
 * KeyStroke, audio, timer, rankings, OBS, SQLite de sesión, licencias.
 *
 * TODO(Etapa 23): implementar connector/, normalizer/ y websocket/.
 */

console.log("[reactstream-bridge] placeholder — Etapa 0 (Foundation)");
